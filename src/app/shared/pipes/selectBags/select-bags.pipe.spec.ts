import { SelectBagsPipe } from './select-bags.pipe';

describe('SelectBagsPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectBagsPipe();
    expect(pipe).toBeTruthy();
  });
});
